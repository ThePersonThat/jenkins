@Library('pipelineLib') _

pipline {
    agent any

    stages {
        stage('Hello world') {
            steps {
                echo "Hello"
                echo gitUtils.getCurrentBranchCli()
            }
        }
    }
}