@Library('pipelineLib') _

pipeline {
    agent any

    options {
        skipDefaultCheckout()
    }

    stages {
        stage('Wipe workspace and checkout') {
            steps {
                checkoutCurrentBranch()
                echo gitUtils.getCurrentBranch()
                echo env.BRANCH_NAME
            }
        }
    }
}