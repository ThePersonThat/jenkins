@Library('pipelineLib') _

pipeline {
    agent any

    stages {
        stage('Hello world') {
            steps {
                echo "Hello"
                echo gitUtils.getCurrentBranchCli()
            }
        }
    }
}